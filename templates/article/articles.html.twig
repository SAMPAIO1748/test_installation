<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Document</title>
	</head>
	<body>

		{% for article in articles %}
			<h1>{{ article.title }}</h1>
			<p>{{ article.content }}</p>
			<h3>{{ article.date|date("d/m/Y")}}</h3>
			<h3>{{ article.category.name }}</h3>
			<h3>{{ article.tag.name }}</h3>
			<h2>
				<a href="{{ path('article_update', {'id' : article.id}) }}">Modifier l'article</a>
			</h2>
			<h2>
				<a href="{{ path('article_delete', {'id' : article.id}) }}">Supptimer l'article</a>
			</h2>
			<hr>
		{% endfor %}
		<h2>
			<a href="{{ path('article_create') }}">Ajouter un article</a>
		</h2>
	</body>
</html></body></html>
